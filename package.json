{
    "name": "bcs-app",
    "description": "butter chicken supper app",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "eslint": "^8.18.0",
        "jest": "^26.6.3"
    },
    "scripts": {
        "build": "sam build",
        "deploy": "sam deploy",
        "start": "sam local start-api --template template.yaml --docker-network lambda-local --skip-pull-image -n env.json",
        "start-dynamo": "docker-compose up -d",
        "stop-dynamo": "docker-compose down",
        "test": "jest"
    }
}
