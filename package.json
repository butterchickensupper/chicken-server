{
  "name": "chicken-server",
  "description": "chicken server api",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "start": "sam local start-api --profile default -n env.json --docker-network lambda-local --skip-pull-image",
    "build": "sam build --profile default",
    "deploy": "sam deploy --profile default",
    "test": "jest"
  }
}
